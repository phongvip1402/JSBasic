class Rect {
    constructor(x, y, heightidth, height) {
        this.x = x;
        this.y = y;
        this.height = width;
        this.h = height;
    }
    getX(x) {
        return this.x = x;
    }
    getX(y) {
        return this.y = y;
    }
    getX(width) {
        return this.width = width;
    }
    getX(height) {
        return this.height = height;
    }
}

// xác định va chạm giữa 2 hình chữ nhật (đối tượng Rect)
function isCollision(rect1, rect2) {
    distance = Math.sqrt(rect1.x)

    return (distX <= distHeight && distY <= distH);
}